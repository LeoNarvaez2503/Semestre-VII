apiVersion: apps/v1
kind: Deployment
metadata:
  name: vehiculos-service
  namespace: floresguamanmoralesnarvaez
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vehiculos-service
  template:
    metadata:
      labels:
        app: vehiculos-service
    spec:
      containers:
        - name: vehiculos-service
          image: vehiculos-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: vehiculos-service-config
            - secretRef:
                name: vehiculos-service-secrets
          readinessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 3000
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 5
          resources:
            requests:
              memory: "32Mi"
              cpu: "10m"
            limits:
              memory: "128Mi"
              cpu: "75m"
